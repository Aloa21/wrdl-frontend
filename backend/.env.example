# Backend Environment Variables
# Copy this to .env and fill in your values
# For AWS App Runner: Configure these in the App Runner Console > Service > Configuration > Environment variables

# The private key of the resolver wallet
# This wallet signs game results - keep it SECRET!
# The address of this wallet must match the resolver in game config
RESOLVER_PRIVATE_KEY=your_private_key_here

# Server secret for word randomization
# Generate once: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# IMPORTANT: Keep this constant in production or words will change on restart
SERVER_SECRET=your_server_secret_here

# Server port (optional, defaults to 3001)
# App Runner uses 8080 by default
PORT=3001

# Node environment (development or production)
NODE_ENV=production
